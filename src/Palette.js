import React, { Component } from 'react'

const Palette = ({ colors }) => {
   console.log(colors)
      return (
         <div>
            <h1>Palette</h1>
            {}
         </div>
      )
}

export default Palette